spec:
  id: test-bad-version
  version: "v1.0"
  status: draft
  tier: 2

  context:
    system: Test system

  objective:
    summary: This spec has an invalid version format.

  constraints:
    - id: C-01
      description: "MUST have valid semver"

  acceptance_criteria:
    - id: AC-01
      description: "Should fail version pattern validation"
