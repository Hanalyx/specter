spec:
  id: test-extra-field
  version: "1.0.0"
  status: draft
  tier: 2
  unknown_field: "this should not be here"

  context:
    system: Test system

  objective:
    summary: This spec has an unknown field that should be rejected.

  constraints:
    - id: C-01
      description: "MUST reject unknown fields"

  acceptance_criteria:
    - id: AC-01
      description: "Should fail additionalProperties validation"
