spec:
  id: test-bad-constraint-id
  version: "1.0.0"
  status: draft
  tier: 2

  context:
    system: Test system

  objective:
    summary: This spec has a constraint with an invalid ID format.

  constraints:
    - id: c1
      # Invalid: should be C-01 format, not c1
      description: "MUST use proper constraint ID format"

  acceptance_criteria:
    - id: AC-01
      description: "Parser should reject invalid constraint ID patterns"
