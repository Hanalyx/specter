# Build output
bin/
dist/
specter-linux-*
specter-darwin-*
specter-windows-*

# Stray binary from a bare `go build` at the package root.
# Canonical build path is bin/specter (see Makefile). A plain `go build`
# (without -o) drops the binary here matching the dir name — ignore it so
# it can't get committed by accident.
/specter

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Test coverage
coverage.out

# Dogfood-strict pipeline artifacts (generated per-run by make dogfood-strict)
.specter-results.json
.specter-go-results.json
vscode-extension/junit.xml

# Claude Code instructions
CLAUDE.md

# Codex / agent instructions
AGENTS.md

# Research (internal)
research/

# Local docs not yet ready to ship
docs/DOC_STYLE_GUIDE.md
docs/roadmap/

# Design records, local-only as of 2026-08-16
docs/design/

# Planning surface — local-only as of 2026-05-07
BACKLOG.md
