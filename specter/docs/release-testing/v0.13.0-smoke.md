# v0.13.0 Feature Smoke Test

End-to-end CLI exercise of each new v0.13 feature against synthetic
fixture workspaces (with one cross-check against the Specter repo for
the `resolve dependents` query).

- binary: `/home/rracine/projects/spec-dd/specter/bin/specter`
- host commit: `681cb43`
- date: 2026-05-17

## Results

- **PASS** D1.1 — real trust_level key removed
- **PASS** D1.2 — description prose 'trust_level: high' line preserved
- **PASS** D1.3 — description prose 'trust_level: medium' line preserved
- **PASS** D2.1 — warning names the offending value '"pass"'
- **PASS** D2.2 — warning names the documented enum
- **PASS** D2.3 — --json output includes invalid_status_warnings field
- **PASS** D3.1 — --json exits non-zero on zero-tolerance violation (matches text mode)
- **PASS** D3.2 — --json exit code matches text-mode exit code (2 vs 2)
- **PASS** C2.1 — diff coverage names my-spec in delta output
- **PASS** C4.1 — resolve dependents lists at least one dependent of spec-parse
- **PASS** C5.1 — reverse non-JSON emits 'Found N constraints' summary line
- **PASS** C6.1 — reverse non-JSON emits 'specter explain' handoff line
- **PASS** C6.2 — reverse --json suppresses prose summary line
- **PASS** B.1 — sync --strictness flag registered in CLI
- **PASS** F3.1 — check --test reports unreachable_annotation diagnostic
- **PASS** A4.1 — @reachable manual off-switch suppresses the unreachable_annotation diagnostic

## Tally

- **PASS**: 16
- **FAIL**: 0

