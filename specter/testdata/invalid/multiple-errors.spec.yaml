spec:
  # Missing 'id' (error 1)
  version: "not-semver"
  # Invalid version pattern (error 2)
  status: draft
  tier: 2

  context:
    system: Test system

  objective:
    summary: This spec has multiple validation errors to test error collection.

  constraints:
    - id: C-01
      description: "MUST collect all errors"

  acceptance_criteria:
    - id: AC-01
      description: "Parser should return multiple errors, not fail on first"
