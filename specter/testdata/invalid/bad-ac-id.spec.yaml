spec:
  id: test-bad-ac-id
  version: "1.0.0"
  status: draft
  tier: 2

  context:
    system: Test system

  objective:
    summary: This spec has an acceptance criterion with an invalid ID format.

  constraints:
    - id: C-01
      description: "MUST use proper AC ID format"

  acceptance_criteria:
    - id: ac1
      # Invalid: should be AC-01 format, not ac1
      description: "Parser should reject invalid AC ID patterns"
